import 'package:flutter/material.dart';

class MySavedMeals extends StatelessWidget{
  const MySavedMeals({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return CustomScrollView(
      slivers: [],
    );
  }
}
